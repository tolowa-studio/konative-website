# Railway environment variables for konative-workers
# Set these in the Railway project dashboard

# Target site URL (no trailing slash)
SITE_URL=https://konative.com

# Shared cron authentication secret — must match CRON_SECRET in the Vercel project
CRON_SECRET=
